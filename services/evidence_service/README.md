# Evidence Service

Port `8018`. Sealed originals, working copies, hashing, redaction, holds and manifests.

Owns its database. No cross-service SQL. Use HTTP, Temporal and metadata-only CloudEvents.
