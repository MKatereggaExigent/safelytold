# Tenancy Service

Port `8010`. Tenants, legal entities, regions, organisational units, deployment metadata.

Owns its database. No cross-service SQL. Use HTTP, Temporal and metadata-only CloudEvents.
