'use client';
import {useEffect,useState} from 'react';
import {Alert,Badge,Button,DataTable,EmptyState,Field,Input,PageHeader,Panel,Select,StatusPill} from '@safelytold/ui/components';
import {useSession,useToast} from '@safelytold/ui/context';
import {createTenant,isSuperuser,listTenants,type TenantView} from '../../lib/admin';
export default function AdminPage(){
 const {session}=useSession();const {push}=useToast();const [rows,setRows]=useState<TenantView[]>([]);const [name,setName]=useState('');const [slug,setSlug]=useState('');const [region,setRegion]=useState('af-south-1');const allowed=isSuperuser(session);
 async function load(){if(!allowed)return;try{setRows(await listTenants(session))}catch(e){push(e instanceof Error?e.message:'Could not load tenants','danger')}}
 useEffect(()=>{void load()},[session.accessToken]);
 async function create(){try{await createTenant({display_name:name,slug,home_region:region},session);setName('');setSlug('');await load()}catch(e){push(e instanceof Error?e.message:'Tenant creation failed','danger')}}
 return <main className="shell"><PageHeader eyebrow="Administration" title="SaaS tenant administration" subtitle="Platform administration does not grant access to tenant cases."/>{!allowed?<Alert tone="danger" title="Platform super-admin required">A verified Keycloak platform_super_admin role and allowlisted email are required.</Alert>:<><Alert tone="info" title="Separation of duties">Tenant provisioning is isolated from case assignments and identity-vault approval.</Alert><div className="split"><Panel title="Onboard tenant"><Field label="Organisation"><Input value={name} onChange={e=>setName(e.target.value)}/></Field><Field label="Slug"><Input value={slug} onChange={e=>setSlug(e.target.value)}/></Field><Field label="Data region"><Select value={region} onChange={e=>setRegion(e.target.value)}>{['af-south-1','eu-west-1','us-east-1'].map(x=><option key={x}>{x}</option>)}</Select></Field><Button onClick={create}>Create tenant</Button></Panel><Panel title="Administrative controls"><ul><li>Staff identities and grants are managed in Identity & access.</li><li>Provider channel coverage is managed in Operations.</li><li>Case access requires a separate assignment and purpose.</li></ul></Panel></div><Panel title="Tenants" padded={false}><DataTable keyField="id" empty={<EmptyState title="No tenants" description="Onboard a tenant above."/>} columns={[{key:'display_name',label:'Organisation',render:r=><strong>{r.display_name}</strong>},{key:'slug',label:'Slug',render:r=><span className="mono">{r.slug}</span>},{key:'home_region',label:'Region',render:r=><Badge>{r.home_region}</Badge>},{key:'status',label:'Status',render:r=><StatusPill status={r.status}/> }]} rows={rows}/></Panel></>}</main>
}
